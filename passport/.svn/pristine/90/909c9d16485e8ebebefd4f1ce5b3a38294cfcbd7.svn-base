package cn.easywed.stargate.service;

import java.util.List;
import java.util.Map;

import cn.easywed.stargate.dao.persistence.User;

public interface IUserService {
		
	//注册
	int register(User user);
	
	//查询用户
	User getById(long uid);
	
	//批量查询
	List<User> getUsers(Map condition);
	
	//更新用户状态
	int updateUser(User user);
	
	//用户登录 TODO 需定义返回类型
	User getUserInfo(String mobile,String password);
	
	//用户登出
	int logout(long uid);
	
	//验证密码
	int checkPassword(String mobile,String password);
	
	//查询用户总量
    int queryUserListCountByConditions(Map<String, Object> map);

    //查询用户数量
    List<User> queryUserListByConditions(Map<String, Object> map, int pageNo, int pageSize);

	//根据parentId查询下属用户信息
	List<User> getByparentId(long parentId);	
}
